apiVersion: 1

datasources:
  - name: MQTT Discovery (fixture)
    uid: mqtt-discovery
    type: grafana-mqtt-datasource
    access: proxy
    editable: true
    jsonData:
      uri: tcp://broker:1884
      discoveryMode: true
      rootTopic: pump/system/#
